{
  "name": "@mkaliezz/dsh-plugin-firewall",
  "version": "0.2.0",
  "description": "Offline-first static admission scanner for DeepSeek Harness plugins.",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test lib/tests/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MkaliezZ/dsh-plugin-firewall.git"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "devDependencies": {
    "@types/node": "22.10.0",
    "typescript": "5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
